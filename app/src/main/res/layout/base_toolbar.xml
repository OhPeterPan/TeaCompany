<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:background="@android:color/white"
    android:navigationIcon="@mipmap/ic_app_back"
    app:navigationIcon="@mipmap/ic_app_back">

    <TextView
        android:id="@id/tvActTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:text="标题"
        android:textColor="@android:color/black"
        android:textSize="@dimen/baseTitleSize" />

    <ImageView
        android:id="@+id/ivActRightSetting"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_gravity="right"
        android:paddingLeft="@dimen/padding9"
        android:paddingRight="@dimen/padding9"
        android:src="@mipmap/ic_setting"
        android:visibility="visible" />

    <ImageView
        android:id="@+id/ivActRightSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_gravity="right"
        android:layout_marginRight="@dimen/margin6"
        android:paddingLeft="@dimen/padding9"
        android:paddingRight="@dimen/padding9"
        android:src="@mipmap/ic_search"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tvTitleRight"
        style="@style/textCommentParent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right"
        android:padding="@dimen/padding12"
        android:text="新增"
        android:visibility="gone" />

</android.support.v7.widget.Toolbar>